import common from "./common.flex.js";

// Flex Greeting Template - สำหรับตอบกลับผู้ใช้เมื่อทักทาย
function greetingFlex() {}

// Flex Followme - สำหรับตอบกลับผู้ใช้เมื่อติดตามใหม่
function followmeFlex() {}

// Flex Followup - สำหรับตอบกลับผู้ใช้หลังจากต้อนรับ
function followupFlex() {}

// Flex Unknown - สำหรับตอบกลับผู้ใช้เมื่อไม่รู้คำสั่ง
function unknownFlex() {}

export { greetingFlex, followmeFlex, followupFlex, unknownFlex };
